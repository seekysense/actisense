# ── Embedding service ──────────────────────────────────────────────
EMBEDDING_API_KEY=gln_qrKN0_OxXKIFK5GRvju6VatfXSOvr-Slx67qFpDFoM6q8TbgiOiBKWESn2n5-le1
EMBEDDING_BASE_URL=https://api-gb10.elettra.ai/v1
EMBEDDING_MODEL=Galene/Embedding-Vision

# ── LLM Vision (OpenAI-compatible) ────────────────────────────────
LLM_BASE_URL=https://api-gb10.elettra.ai/v1
LLM_API_KEY=gln_qrKN0_OxXKIFK5GRvju6VatfXSOvr-Slx67qFpDFoM6q8TbgiOiBKWESn2n5-le1
FAST_MODEL=Galene/LLM
REASONING_MODEL=Galene/LLM
LLM_USEREASONING=true
LLM_TIMEOUT=600
LLM_CONTEXT_WINDOW=256000
EMB_CONTEXT_WINDOW=8000
LLM_THINKING=false

# ── Frame processing ──────────────────────────────────────────────────────────
FRAME_SIZE_EMBEDDER=224
EMBED_FALLBACK_SIZE=128
FRAME_SIZE_LLM=768
EMBED_FPS=2
EMBED_WINDOW_SEC=4
EMBED_MAX_WINDOWS=5
EMBED_MIN_FRAME_DIFF=0.10
EMBED_TOP_K=1
LLM_MAX_CALLS=5
LLM_FRAME_SIZE_SEND=336
LLM_JPEG_QUALITY_SEND=80

# FRAME_DEBUG_DIR intentionally left empty — disables debug frame writing in production
FRAME_DEBUG_DIR=

# ── Axis / VAPIX ───────────────────────────────────────────────────
AXIS_DEFAULT_USER=actisense
AXIS_DEFAULT_PASS=1rc0QqEXCw5M9i
AXIS_TEST_CAMERA_URL=http://10.46.67.5/
AXIS_TEST_EVENT_ID=cabinet
AXIS_USERNAME=actisense
AXIS_PASSWORD=1rc0QqEXCw5M9i
AXIS_DOWNLOAD_FPS=4
AXIS_POLL_INTERVAL_SEC=10

# ── Storage (container-internal paths mapped via Docker volumes) ───────────────
CLIP_TEMP_DIR=/tmp/vsa_clips
CLIP_STORAGE_DIR=/data/clips
CLIP_TEMP_TTL_HOURS=24
LANCEDB_PATH=/data/lancedb

# ── Output ────────────────────────────────────────────────────────────────────
# Engine sends alerts to the API container on the internal Docker network
WEBHOOK_DEFAULT_URL=http://vsa-api:8000/api/internal/alert

# ── Runtime ───────────────────────────────────────────────────────────────────
QUEUE_MAX_WORKERS=4
QUEUE_MAX_DEPTH=100
LOG_LEVEL=INFO
DEBUG_MODE=false

# ── API server ────────────────────────────────────────────────────────────────
API_HOST=0.0.0.0
API_PORT=8000

# ── Auth ──────────────────────────────────────────────────────────────────────
API_USERNAME=admin
API_PASSWORD=chaVatfXSOvr-Slx67qFpoiY_gvju6VatfXSOvr-Slx67qsRikgdswErcvbHiY_gi3df3Xed3ws3-33n
JWT_SECRET=chVatfXSOvr-Slx67qFpera3ed3Rfeth-pythVatfXSOvr-Slx67qFpts-tokVatfXSOvr-Slx67qFpfe-32

# ── Config Management API ─────────────────────────────────────────────────────
# Generate: python -c "import secrets; print(secrets.token_urlsafe(32))"
CONFIG_API_KEY=cgLkoiY_gvju6VatfXSOvr-Slx67qsRikgdswErcvbHiY_gqFpts-tokVatfX

# ── CORS ──────────────────────────────────────────────────────────────────────
# Comma-separated origins allowed for CORS. Swagger UI works from the same nginx
# origin, so only add external tool origins here (Postman, separate dev hosts).
CORS_ORIGINS=http://localhost:5173,http://localhost:4173,https://frameiq.infinitearea.com

# ── Host data path (used by docker-compose volumes bind mount) ────────────────
DATA_DIR=./data
